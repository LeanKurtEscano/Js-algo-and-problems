const wakeUp = (status) => {
    return !status;
}
 const taskForToday = async() => {
    let wokenUp = false;
    let isWakeUp = wakeUp(wokenUp);
    if(isWakeUp){
        console.log("Person has woken up");
    }
}

taskForToday();



const stillSleeping = () => {

}

const eatBreakFast = () => {
    console

}